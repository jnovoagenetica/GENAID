# === Core API en Lambda ===
fastapi==0.111.0
starlette==0.37.2
mangum==0.19.0
# Runner local (no afecta Lambda)
uvicorn==0.30.1

# === AWS SDK ===
boto3==1.35.33
aws-lambda-powertools==2.35.1

# === Utilidades / Config ===
python-dotenv==1.1.1
requests==2.32.3
httpx==0.27.2

# === Validación ===
pydantic==2.9.2
pydantic-core==2.23.4
annotated-types==0.7.0

# === Auth (JWT Cognito) ===
python-jose==3.3.0
cryptography==45.0.5
rsa==4.9
pyasn1==0.6.1

# === Deps usadas por los routers / utilidades del proyecto ===
# 'humps' se instala como 'pyhumps'
pyhumps==3.8.0
python-ulid==1.1.0
PyYAML==6.0.2
tenacity==8.3.0
retry==0.9.2

# === Postgres (si tu código lo importa) ===
pg8000==1.31.2
scramp==1.4.5

# === Formularios / archivos (FastAPI) ===
python-multipart==0.0.20

# === PDF / Imágenes ===
pymupdf==1.24.9     # proporciona 'import fitz'
pillow==10.4.0      # thumbs/PNG si conviertes pixmaps

# === Web search ===
duckduckgo_search==6.2.8
