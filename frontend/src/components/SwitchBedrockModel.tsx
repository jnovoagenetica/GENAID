import { BaseProps } from '../@types/common';

type Props = BaseProps;

// Ocultamos completamente el switch de modelos
const SwitchBedrockModel: React.FC<Props> = () => {
  return null;
};

export default SwitchBedrockModel;